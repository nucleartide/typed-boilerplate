/*
import type { Todo as Model } from '../types/todo';

type Props = Todo & {
};

// <YourComponent> does something.
const YourComponent = (p: Props) => (
  <div>hello world</div>
);

export default YourComponent;
*/
